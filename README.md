# Lab6

using Flask templates allowing creating of HTML pages outside of the Python code then rendering the template from within Python.

Flask functionality for creating a secure login form and associated files for a web site.

Enhance the Web site with additional functionality to include images, tables and a Form using Python flask. 

Additional routes allows a user to register and login to a web site. 
Additional security considerations include other routes (beyond the register route) will not be accessible until a successful login has occurred.


Includes:
A user registration form
A user login form
A password complexity enforced to include at least 12 characters in length, and include at least 1 uppercase character, 1 lowercase character, 1 number and 1 special character.
